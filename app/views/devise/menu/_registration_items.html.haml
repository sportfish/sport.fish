- if user_signed_in?
	/ edit profile or similar
	/ = link_to('Edit registration', edit_user_registration_path, class: "sign-up")
- else
	= link_to('Sign Up', new_user_registration_path, class: "sign-up")